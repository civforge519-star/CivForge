{
  "name": "worldbox-cloud-mvp",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w apps/worker\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w apps/worker && npm run build -w apps/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}

