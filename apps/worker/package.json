{
  "name": "worldbox-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "@vitest/ui": "^4.0.18",
    "typescript": "^5.5.4",
    "vitest": "^4.0.18",
    "wrangler": "^4.62.0"
  }
}
